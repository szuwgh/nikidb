use crate::datafile::DataFile;
use 
pub struct DB {
    // active file:
    active_data_file: DataFile,
}

impl DB {
    pub fn open(dirPath: &str) {
        create
    }
}

#[cfg(test)]
mod tests {
    use super::*;
}
